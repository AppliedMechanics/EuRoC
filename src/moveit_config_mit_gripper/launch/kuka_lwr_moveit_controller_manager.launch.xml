<launch>

  <!-- Set the param that trajectory_execution_manager needs to find the controller plugin -->
  <param name="moveit_controller_manager" value="moveit_controller_manager_kuka_lwr/MoveItControllerManagerKukaLwr"/>

  <!-- The rest of the params are specific to this plugin -->
  <rosparam file="$(find moveit_config_mit_gripper)/config/controllers.yaml"/>

</launch>
